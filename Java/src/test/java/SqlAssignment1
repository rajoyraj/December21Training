1.
SELECT * FROM sakila.city ORDER BY city DESC limit 10;
2.
SELECT * FROM sakila.film WHERE title='AIRPLANE';
3.
SELECT MAX(amount) AS HighestPaymentAmount FROM sakila.payment;
4.
SELECT * FROM sakila.customer WHERE store_id=1;
5.
SELECT * FROM sakila.payment INNER JOIN sakila.customer ON sakila.payment.customer_id=sakila.customer.customer_id where email="NANCY.THOMAS@sakilacustomer.org";
6.
SELECT film_info FROM sakila.actor_info WHERE first_name="BOB" AND last_name="FAWCETT";

7. In this one since we have to pass filmID as an argument so i used the SELECT query to get the film_id for the given film and then i displayed the inventory.
SELECT film_id FROM sakila.film WHERE title="ALIEN CENTER";
CALL film_in_stock(15,2,@count);

